name: CI/CD Pipeline

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List files
        run: ls -l

  deploy:
    runs-on: self-hosted  # Your laptop
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy HTML/JS
        run: |
          echo "Deploying files to local server folder..."
          copy index.html C:\DeploymentFolder\
          copy script.js C:\DeploymentFolder\
